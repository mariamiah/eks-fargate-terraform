# EKS_FARGATE_TERRAFORM
- Clone repository
- Configure aws cli and add an aws profile at `~/.aws/credentials`
- Run the commands below
```sh
    terraform init
    terraform plan
    terraform apply
```
- Destroy resources after testing
```sh
    terraform destroy
```